# Copy to .env; load with `uv run --env-file .env <command>`.

# alembic dev database
DATABASE_URL=sqlite+aiosqlite:///pravda.db

# test browser endpoint
PRAVDA_TEST_BROWSER_WS_URL=ws://localhost:9222
